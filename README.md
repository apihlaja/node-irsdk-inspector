# node-irsdk-inspector

Shows iRacing session info and telemetry using [node-irsdk](https://github.com/apihlaja/node-irsdk).

## Install

1. clone git repo
2. `npm install`
3. `npm start`

Use `npm run server` for quick start.

